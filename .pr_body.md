# Summary

Adds a surgical prompt update for dictation formatting behavior so quote commands and spoken punctuation words are handled more reliably while preserving existing isolation and override precedence.

## Type of Change

- [ ] Bug fix
- [x] Feature
- [ ] UI change
- [ ] Refactor
- [ ] Documentation
- [x] Test-only change
- [ ] Build or release change

## Testing

- [x] `npm run check`
- [x] `npm run lint`
- [x] `npm run test:rust`
- [ ] `npm run test:smoke`
- [ ] `npm run test:smoke:state`
- [x] Playwright or manual UI verification, if applicable

## Privacy and Security

- [x] No API keys, personal data, local private paths, screenshots with private text, or sensitive logs are included.
- [x] API keys remain outside SQLite and are not logged.
- [x] Clipboard, hotkey, database, or provider behavior changes are explained below.

Notes:

- Prompt-level behavior only. No runtime credential handling changes.
- Existing Playwright smoke scripts for Settings > Advanced selector failed before PR completion:
  - `tests/smoke/playwright-test-ui.cjs`
  - `tests/smoke/playwright-test-state.cjs`
  - `tests/smoke/playwright-test-fixes.cjs`

## UI Evidence

Not applicable.

## Reviewer Notes

- Prompt updates are intentionally scoped to `FORMATTING COMMANDS` guidance in `src-tauri/src/api/prompts.rs`.
- Added unit tests for prompt content contracts (quote commands, spoken punctuation mapping guidance, context guardrails, and override semantics).
- This branch includes current worktree changes present at branch creation time.
